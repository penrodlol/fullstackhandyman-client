import { InlineDirective } from './inline.directive';

describe('InlineDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineDirective();
    expect(directive).toBeTruthy();
  });
});
